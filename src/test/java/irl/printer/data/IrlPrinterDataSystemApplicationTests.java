package irl.printer.data; // Keep this as is if correct

import com.example.SpringBootBaseCodeApplication; // Import your main application class
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = SpringBootBaseCodeApplication.class) // Explicitly specify main class
class IrlPrinterDataSystemApplicationTests {

    @Test
    void contextLoads() {
    }
}
